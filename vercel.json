{
  "buildCommand": "pnpm --filter @workspace/task-manager run build",
  "outputDirectory": "artifacts/task-manager/dist/public",
  "framework": "other",
  "rewrites": [
    {
      "source": "/:path((?!.*\\.).*)",
      "destination": "/index.html"
    }
  ]
}
